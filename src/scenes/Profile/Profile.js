import React from 'react';
import styled from 'styled-components';

const ProfileWrapper = styled.div`
  padding: 10px;
`;

const Profile = () => <ProfileWrapper>Profile</ProfileWrapper>;

export default Profile;
